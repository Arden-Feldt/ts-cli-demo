{
  "name": "ts-devcontainer",
  "description": "A sample TypeScript CLI project.",
  "main": "dist/index.js",
  "bin": {
    "ts-cli-demo": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "postbuild": "chmod +x dist/index.js && npm link",
    "start": "node dist/index.js"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "@types/node": "^22.12.0",
    "typescript": "^5.7.3",
    "prettier": "^3.5.1"
  },
  "dependencies": {
    "commander": "^13.1.0"
  }
}
